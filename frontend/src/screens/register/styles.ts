import { CSSProperties } from 'react'

export const styleLayout: CSSProperties = {
  minHeight: '100vh',
}

export const styleLayoutHeader: CSSProperties = {
  background: '#fff',
}

export const styleLayoutContent: CSSProperties = {
  padding: '25px 50px',
}
